name: kooka_sing
description: Kooka Sing - Duolingo-style singing learning app built with Flutter
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  # Keeping initial stack minimal; add packages later as needed
  # flutter_riverpod: ^2.5.1
  # go_router: ^14.2.1
  # lottie: ^3.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
  # assets:
  #   - assets/lessons/



