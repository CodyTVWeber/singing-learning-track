name: kooka_sing
description: Kooka Sing - Duolingo-style singing learning app built with Flutter
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  # Navigation
  go_router: ^14.2.1
  
  # Local storage
  hive: ^2.2.3
  hive_flutter: ^1.1.0
  
  # State management (later if needed)
  # flutter_riverpod: ^2.5.1
  
  # Animations (deferred to M5+)
  # lottie: ^3.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
  assets:
    - img/



